.DS_Store
*.swp
*.bak
*.bak.*

# Claude Code local (machine-specific) settings — never publish
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# LLM eval run records — local artifacts (model + timestamp + outcomes), not fixtures
tests_eval/results/
